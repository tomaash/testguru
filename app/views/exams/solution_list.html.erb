<% sorting = @exam.sorting=="reverse" ? -1 : 1 %>
<%  @exam.questions.sort_by{|q| q.id*sorting}.each_with_index do |question,i| %>
  <% correct_answers = question.answers.select{|x| x.correct} %>
  <%= correct_answers.map{|x| x.choice}.join(',') %> <br/>
<% end %>

